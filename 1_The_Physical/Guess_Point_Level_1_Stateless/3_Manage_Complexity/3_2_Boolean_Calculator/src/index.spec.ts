
import { BooleanCalculator } from './index';

describe('boolean calculator', () => {
    it('Create a boolean calculator', () => {
        expect(typeof(BooleanCalculator)).toBe("function");
    })
})
