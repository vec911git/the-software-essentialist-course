import { isPasswordValid } from './index'

describe('password validator', () => {

    it('Write a function for validating passwords', () => {
        expect(typeof(isPasswordValid)).toBe("function");
    })

})

